version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo "Installing dependencies..."
      - npm install
  build:
    commands:
      - echo "Build complete. Packaging for Elastic Beanstalk deployment..."
artifacts:
  files:
    - '**/*'
  discard-paths: no
